<%= form_with model: @book, url: '/books', method: :post do |f| %>
    <h4>Book's Title</h4>
    <%= f.text_field :title %>

    <h4>Feeling</h4>
    <%= f.text_area :body %>

    <%= f.submit :'Submit' %>
<% end %>